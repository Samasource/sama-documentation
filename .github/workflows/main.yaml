name: Upload Documents

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: arn:aws:sts::596059236576:assumed-role/AWSReservedSSO_DeveloperAccess_760de31ca6d3d09e/dricher@samasource.org
          aws-region: us-east-1
